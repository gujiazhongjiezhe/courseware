let initState = {
  list:[1,2,3,4]
}

function reducer(state = initState,action){
        //          默认值           {}
        //         {list:[1,2,3]}  {type:ADD_MIN,payload:2}
      switch(action.type){
        
      }
      return state;
}

export default reducer;