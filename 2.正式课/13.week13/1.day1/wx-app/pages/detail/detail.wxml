<view class="detail">
    <view class="h1">课程详情</view>
    <view class="h2">{{title}}</view>
    <image src="{{img}}" ></image>
    <view class="bot_box" >
        {{count}}
        <button bindtap="add">加入购物车</button>
        <button bindtap="buy">立即购买</button>
    </view>
</view>
