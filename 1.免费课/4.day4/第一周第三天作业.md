# 第一周第三天作业(新)

-  一、输出下面的运行结果

    + 1、
        ```
        let a = 12;
        if (a >= 0 && a <= 20) {
            if (a % 2 === 0) {
                a += 2;
            }
        } else {
            a -= 2;
        }
        console.log(a);
        ```


    + 2、
        ```
        let ary = ['erYa', '珠峰', '培训', true, false];
        for (var i = 0; i <ary.legnth; i++) {
            console.log(ary[i]);
            break;
        }
        console.log(i);
        ```

    + 3、
        ```
        for (var i = 10; i > 0; i--) {
            continue;
            console.log(i);
        }
        console.log(i);
        ```
    + 4、
        ```
        for (var i = 1; i <= 10; i += 2) {
            if (i <= 5) {
                i++;
                continue;
            } else {
                i -= 2;
                break;
            }
            i--;
            console.log(i);
        }
        console.log(i);
        ```
    + 5、
        ```
        for (var i = 3; i < 12; i++) {
            if (i < 3) {
                i++;
                break;
            }
            if (i > 9) {
                i += 2;
                continue;
            }
            i--;    
        }
        console.log(i);
        ```


    + 6、
        ```
        var i=2;
        console.log(i++ == ++i) ;
        console.log(++i == i++); 
        console.log(i);

        ```
    + 7、
        ```
        var a = 1;
        var num = (a++) + (++a) + (a++) - (a--) - (--a);
        console.log(num);

        ```

- 二、输出下面的结果
    ```
    1、 !(!"Number(undefined)");
    2、 isNaN(parseInt({})) + Number([1]) + parseFloat(undefined);
    3、 Boolean(Number("")) + !isNaN(Number(null)) + Boolean("parseInt([])") +  !null;
    4、 parseFloat("1.6px") + parseInt("1.2px") + parseInt(null).toString();
    5、 isNaN(Number(!!Number(parseInt("0.8"))));
    6、 0 && 1 || '1' && NaN || 5 && '萨瓦迪卡';

    ```

- 三、 输出以下结果

    ```
    [] == [] // false
    '' == 0 // true
    [0] == false // true
    true == 1 // true
    ({}) == '' // false
    [] == 0 // true
    0 == null // false
    '{}' == 1 // false
    undefined == null // true
    null === undefined  // false
    ({name:1}) === ({name:1}) // false
    undefined == 0 // false
    false == null // false
    '1' === 1 // false

    ```

- 四、输出以下结果(遇见报错不用管，继续执行)
    + 1、
        ```
        let age = null;
        let name = 'weiWei';
        let obj = {
            name: name,  // 这个就没了
            age: age, // null
            name: 'name',
            3: 'erYa'
        };
        console.log(obj); // {age:null,name:'name',3:'erYa'}
        obj.name = name; // 就是把obj中的属性名name对应的属性值给为 'weiWei'
        console.log(obj.name); // 'weiWei' 此处的name不是变量，他就是对象中的属性名
        obj = {}; // 给变量obj重新赋值了一个新的空间地址(af0)
        console.log(obj.name); // un
        obj.name = name; // 给新的堆内存(对象)新增键值对，属性名是name，属性值是'weiWei'
        obj.ifantName = 'liaoLiao'; //  // 给新的堆内存(对象)ifantName 属性值是 'liaoLiao'

        // {name:'weiWei',ifantName:'liaoLiao'}
        console.log(obj.name, obj.ifantName, obj)
        ```
    + 2、

        ```
        let n = [10, 20];
        let m = n;
        let x = m;
        m[0] = 100; 
        x = [30, 40];  
        x[0] = 200;
        m = x;
        m[1] = 300;
        n[2] = 400;
        console.log(n, m, x);

        ```
    + 3、
        ```
          let a = {
            n: 1
        };
        let b = a;
        a.x = a = { // 像这种连续等于要从左往右依次赋值
            n: 2
        };
        console.log(a.x, b);
        ```

